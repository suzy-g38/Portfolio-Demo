# Stage 3: Next.js (App Router)

This branch (`03-nextjs`) has two sibling folders:

- **[`starter/`](starter/)** — boilerplate with `TODO` comments.
  Students build here during the session.
- **[`completed/`](completed/)** — the finished, deploy-ready
  reference version.

Compare them any time — they're both checked out at once, right next
to each other, no branch switching needed within this stage.

## Quick start

```bash
cd starter
npm install
npm run dev
```
See [`starter/README.md`](starter/README.md) for what to do, or
[`completed/README.md`](completed/README.md) for the finished version
and how it deploys.

Full workshop materials (session plan, facilitator script, student
guide, deployment steps for all three stages): see the `main` branch.
